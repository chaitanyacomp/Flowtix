/** Superseded by ProductionOperatorIdentityBar in page header — avoid duplicate KPI card. */
export function GreenLevelProductionCurrentWoCard(_props: {
  row?: unknown;
  fmtProdQty?: (n: number, unit?: string | null) => string;
}) {
  return null;
}

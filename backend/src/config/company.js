/**
 * Static company GST state for intra/inter split.
 * Keep this simple (no UI) as requested.
 */

const COMPANY_STATE = {
  name: "Maharashtra",
  code: "27",
};

module.exports = { COMPANY_STATE };


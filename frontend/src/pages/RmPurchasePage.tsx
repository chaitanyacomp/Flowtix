/**
 * RM Purchase hub lives under `rmPurchase/` (list + detail routes).
 * Re-export for references to "RmPurchasePage" / tooling.
 */
export { RmPurchaseListPage as RmPurchasePage } from "./rmPurchase/RmPurchaseListPage";
